@model TechCompilerCo.Models.ContasReceberViewModel

@{
    ViewData["Title"] = "Contas Recebidas";
}

<div class="row">
    <div class="row">
        <div class="col-md-6">
            <span><b>SUA</b></span><br />
            <span><b>MECÂNICA</b></span>
        </div>

        <div class="col-md-6" style="text-align: right; font-size: 20px">
            <span><b>Contas Recebidas</b></span>
        </div>
    </div>

    <br />
    <br />
    <br />

    <div class="row col-md-12">
        <div class="col-md-1">
            <a class="btn btn-sm btn-success" href="@Url.Action("Excel", "ContasReceber")"><i class="fa fa-table"></i> Excel</a>
        </div>

        <div class="col-md-11 pull-right">
            <a href="@Url.Action("Index", "ContasReceber")" class="btn btn-sm btn-primary pull-right" style="border-radius: 8px"><i class="fa fa-door-open"></i> Voltar</a>
        </div>
    </div>

    <br />
    <br />
    <br />

    <div class="row">
        <table class="table col-sm-12 col-md-12 col-lg-12 col-xl-12 col-xxl-12" id="MinhaIndex">
            <thead>
                <tr>
                    @if (Model.UsuarioAdm)
                    {
                        <th></th>
                    }
                    <th>
                        Número Documento
                    </th>
                    <th>
                        Data Vencimento
                    </th>
                    <th>
                        Data Pagamento
                    </th>
                    <th>
                        Preço
                    </th>
                    <th>
                        Serviço
                    </th>
                    <th>
                        Dados Cliente
                    </th>
                </tr>
            </thead>
            <tbody>
                @foreach (var c in Model.Contas)
                {
                    <tr>
                        @if (Model.UsuarioAdm)
                        {
                            <td>
                                <a class="btn btn-xs btn-primary" href="@Url.Action("Edit", "ContasReceber", new { id = c.CodigoCre, contasRecebidas = true } )"><i class="fa fa-edit"></i></a>

                                <a class="btn btn-xs btn-danger" onclick="abrirModalRemoto('@Url.Action("DeletePartial", "ContasReceber", new { id = c.CodigoCre, contasRecebidas = true } )');"><i class="fa fa-trash"></i></a>
                            </td>
                        }
                        <td>
                            @c.NumeroDocumento
                        </td>
                        <td>
                            @c.DataVencimento.ToString("dd-MM-yyyy")
                        </td>
                        <td>
                            @c.StrDtPagamento
                        </td>
                        <td>
                            R$ @c.Valor.ToString("N2", new System.Globalization.CultureInfo("pt-BR"))
                        </td>
                        <td>
                            @c.ServicoCobrado
                        </td>
                        <td>
                            @c.NomeCliente / @c.Documento / @c.Telefone1
                        </td>
                    </tr>
                }
            </tbody>
        </table>
    </div>
</div>